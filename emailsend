import smtplib

# The single prompt
name = input("What is your first name? ")

def send_mailtrap_test(user_name):
    # Standard email formatting for Mailtrap
    sender = "Private Person <from@example.com>"
    receiver = "A Test User <to@example.com>"
    
    message = f"""\
Subject: Script Test Successful
To: {receiver}
From: {sender}

Notification: {user_name} has successfully tested the script!"""

    try:
        # Using your Mailtrap Sandbox credentials
        with smtplib.SMTP("sandbox.smtp.mailtrap.io", 2525) as server:
            server.starttls()
            server.login("b987c5c0f8ebb4", "639253c7bde7bd")
            server.sendmail(sender, receiver, message)
            
        print(f"\nThank you! I emailed Jodie that you have tested her script!")
    except Exception as e:
        print(f"\nError: {e}")

if name:
    send_mailtrap_test(name)
