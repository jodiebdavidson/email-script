import smtplib
from email.message import EmailMessage

# 1. THE ONLY PROMPT
name = input("What is your first name? ")

def send_notification(user_name):
    msg = EmailMessage()
    msg.set_content(f"Success! {user_name} has tested the script.")
    msg['Subject'] = f'Script Test: {user_name}'
    msg['From'] = "fce2022@gmail.com"
    msg['To'] = "crystalforest6@gmail.com"

    try:
        # Using Gmail SMTP with your provided App Password
        server = smtplib.SMTP('smtp.gmail.com', 587)
        server.starttls()
        # Your App Password: zzgfowgmbypavsvu
        server.login("fce2022@gmail.com", "zzgfowgmbypavsvu")
        server.send_message(msg)
        server.quit()
        
        # 2. THE FINAL CONFIRMATION
        print(f"\nThank you! I emailed Jodie that you have tested her script!")
    except Exception as e:
        print(f"\nError: {e}")

if name:
    send_notification(name)
